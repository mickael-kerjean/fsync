[package]
name = "fdrive-mac"
version = "0.1.0"
edition = "2021"

[lib]
name = "fdrive_mac"
crate-type = ["cdylib"]

[dependencies]
fdrive-core = { path = "../fdrive-core" }
tokio = { version = "1", features = ["rt-multi-thread"] }
libc = "0.2"
log = "0.4"
env_logger = "0.11"
